# MCP PTY 本地部署说明

该目录用于 `mcp` 子菜单中的 `mcp_pty` 入口与资源文件（`mcp_pty.sh` / `pty.md`）。
当前仓库不再内嵌 `pty-runner.py` 副本；MCP PTY 服务直接由 PyPI 上的 `pty-mcp` 提供，默认通过 `uvx --from pty-mcp pty-mcp` 运行。
`mcp_pty` 单脚本支持三种用途：本地搭建、关联 opencode、卸载相关配置。

---

## 入口

- 菜单入口：`mcp -> mcp_pty`
- Headless 入口：
  - `bash install.sh mcp_pty`
  - 或安装后：`jb mcp_pty`

---

## 运行模式

执行 `MCP/pty/mcp_pty.sh` 时，支持以下三种模式：

1. `install`（默认）：本地搭建
2. `opencode`：本地搭建 + 关联 opencode
3. `uninstall`：仅卸载 mcp_pty 相关内容（不卸载 uv）

默认模式 `install` 会：

1. 检测并安装 `uv`（若未安装）
2. 立即加载 `~/.cargo/env`（若存在）
3. 校验 `uvx` 可用
4. 准备默认 PTY 包规格：`pty-mcp`

默认不会修改 `~/.config/opencode/opencode.json`。

说明：

- `install` 模式不会在本地落地 `server.py`，只负责准备运行 `pty-mcp` 所需的 `uv/uvx` 环境。
- 默认会按 `uvx` 的解析/缓存策略使用 PyPI 上的 `pty-mcp`。
- 如需固定版本或改用其他来源，可显式传入 `--pty-mcp-spec`，或设置环境变量 `PTY_MCP_SPEC`。

---

## 当前工具能力概览

当前同步后的 PTY server 不再只是最小的 `spawn/read/write` 组合，而是提供了更完整的工具面，主要分为几类。
其中会话类工具默认可用；`pty_signal` 与 admin/全局工具默认隐藏，只有在显式设置对应环境变量时才会暴露：

- 会话创建与控制：`pty_spawn`、`pty_write`、`pty_resize`、`pty_close`
- 输出读取：`pty_read`、`pty_read_until`、`pty_read_until_any`、`pty_read_quiescent`
- 流与偏移读取：`pty_read_stdout`、`pty_read_stderr`、`pty_read_at`
- 交互辅助：`pty_prompt`、`pty_wait`、`pty_status`、`pty_list`
- 可选 control 工具：`pty_signal`（需 `PTY_MCP_ENABLE_CONTROL_TOOLS=1`）
- 可选 admin/运行时工具：`pty_close_all`、`set_default_cwd`、`get_default_cwd`、`set_limits`、`get_limits`、`pty_metrics`、`pty_health`（需 `PTY_MCP_ENABLE_ADMIN_TOOLS=1`）
- 诊断与保护：`confirm_dangerous_command`

说明：

- `pty_read_stdout` / `pty_read_stderr` 需要在 `pty_spawn` 时显式传入 `separate_streams=true`；开启后 stdout/stderr 会改走独立缓冲，不应再假定主 `pty_read` 仍能读到同一份输出。
- 默认策略下，`pty_spawn` 与后续 session 工具都要求传入同一个 `owner`；如需兼容旧调用方，可显式设置 `PTY_MCP_REQUIRE_OWNER=0`。
- 默认策略下，高风险命令必须先调用 `confirm_dangerous_command` 获取 `confirm_token`，再在 `pty_spawn` 中传入 `dangerous_confirm_token`；旧的 `dangerous_confirmed` / `dangerous_justification` 仅在显式设置 `PTY_MCP_DANGEROUS_LEGACY_MODE=1` 时才作为兼容路径保留。

---

## 关联 opencode（模式二）

启用参数：

```bash
bash MCP/pty/mcp_pty.sh --with-opencode
bash MCP/pty/mcp_pty.sh --mode opencode
```

额外会执行：

1. 从远端下载 `MCP/pty/pty.md` 到本地：
   `~/.config/opencode/instructions/pty.md`
2. 原子更新 `~/.config/opencode/opencode.json`：
   - 写入 `mcp["pty-runner"]` 为 `uvx --from pty-mcp pty-mcp`
   - `PATH` 使用 `{env:HOME}/.local/bin:{env:HOME}/.cargo/bin` 前缀，并附加 `LANG/LC_ALL`
   - 追加并去重 `instructions` 路径（目标为 `{env:HOME}/.config/opencode/instructions/pty.md`）

默认会跟随 `pty-mcp` 的最新可解析版本；如需锁定版本，可使用：

```bash
bash MCP/pty/mcp_pty.sh --with-opencode --pty-mcp-spec "pty-mcp==0.2.0"
```

---

## 常用参数

- `--mode <install|opencode|uninstall>`：指定运行模式
- `--with-opencode`：启用 opencode 配置写入
- `--uninstall`：卸载 mcp_pty 相关内容（不卸载 uv）
- `--remote-raw-base <url>`：覆盖远端 raw 基址
- `--local-dir <path>`：覆盖历史本地目录（仅用于兼容旧版清理）
- `--opencode-config <path>`：覆盖 opencode.json 路径
- `--opencode-instruction-path <path>`：覆盖 pty.md 本地路径
- `--pty-mcp-spec <spec>`：覆盖 `pty-mcp` 包规格（默认 `pty-mcp`）
- `--dry-run`：仅打印动作，不实际写入

---

## 卸载模式（模式三）

```bash
bash MCP/pty/mcp_pty.sh --mode uninstall
# 或
bash MCP/pty/mcp_pty.sh --uninstall
```

卸载会清理：

1. 旧版遗留的 `~/mcp/mcp-pty/server.py`（若存在）
2. `~/.config/opencode/instructions/pty.md`
3. `~/.config/opencode/opencode.json` 中的 `mcp["pty-runner"]`
4. `instructions` 中 `pty.md` 的关联路径（含 `{env:HOME}` / `${HOME}` / 绝对路径变体）

不会卸载 `uv`。

---

## 验证建议

1. `uv --version`
2. `uvx --from pty-mcp pty-mcp --help`
3. `opencode mcp list`（应看到 `pty-runner connected`）
4. 对话测试：使用 `pty-runner` 运行 `pwd`，确认 PTY 会话可正常启动并返回当前工作目录。
5. 对话测试：使用 `pty-runner` 运行 `printf 'Hello from uv'` 并读取输出。
6. 如需验证扩展能力，可继续测试：
    - 使用 `pty_status` 查看退出码与缓冲区元数据
    - 使用 `pty_read_until` / `pty_read_until_any` 等待指定文本
    - 在 `pty_spawn` 传入 `separate_streams=true` 后，分别调用 `pty_read_stdout` 与 `pty_read_stderr`
    - 对高风险命令先调用 `confirm_dangerous_command`，再将返回的 `confirm_token` 传给 `pty_spawn`
    - 如需使用 `pty_signal`、`set_limits` 等额外工具，需先在运行环境启用对应的 control/admin 开关

---

## 远端缓存说明

脚本下载远端文件时附带时间戳查询参数（`?_=<unix_ts>`），用于规避 CDN/缓存延迟。
